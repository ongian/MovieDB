import React from 'react';

const Sort = (props) => {
    <select>
        <option value="ascending">Ascending Title</option>
        <option value="descending">Descending Title</option>
    </select>
}